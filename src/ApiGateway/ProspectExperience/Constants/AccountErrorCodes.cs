namespace ApiGateway.ProspectExperience.Constants;

/// <summary>
/// Defines the downstream Account error codes interpreted by the prospect orchestration.
/// </summary>
public static class AccountErrorCodes
{
    /// <summary>
    /// Gets the Account error code returned when the role already exists on the account.
    /// </summary>
    public const string ExistingRole = "ACC021";
}
