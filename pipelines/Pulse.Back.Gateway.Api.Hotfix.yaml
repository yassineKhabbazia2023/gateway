trigger: none

pool:
  vmImage: 'ubuntu-latest'

resources:
  repositories:
  - repository: Pulse
    name: 'Pulse.Operations.Pipelines'
    type: git
    ref: 'refs/heads/Release/v4.0'

extends:
  template: pipelines/hotfix/back.yaml@Pulse
  parameters:
    srcPath: src/ApiGateway/ApiGateway.csproj
    domain: gtw
    name: desktop
    appType: webApp
    id: '01'